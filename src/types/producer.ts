export type Producer = {
    name: string
    gender: string
    dob: string
    bio: string
  }
  
  export type ProducerResponse = {
    _id: string
    name: string
    gender: string
    dob: string
    bio: string
  }
  