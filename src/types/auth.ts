export type SignUpData = {
    number: string;
    name: string;
    password: string;
}
export type LoginData = {
    number: string;
    password: string;
}
export type User = {
    id: string,
    name: string,
    number: string
}
